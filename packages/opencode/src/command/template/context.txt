Use the `context_compile` tool to pull the code-graph slice (definition + callers + references, connected across files) for the following symbol, file/dir path, or plain-language question, then answer using what it returns. Prefer the graph over grep/glob for understanding code.

$ARGUMENTS
